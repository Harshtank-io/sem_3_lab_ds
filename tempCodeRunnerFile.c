

    avg = sum / n;
    printf("the avreage of an array is : %d\n", avg);
    for (i = 0; i < a[i]; i++)
    {
        if (a[i] > avg)
        {
            printf("%d ", a[i]);
        }
    }
}