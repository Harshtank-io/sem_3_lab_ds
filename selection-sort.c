#include <stdio.h>
#define max 5
int n;
void sort()
{
    int i, j, min_idk;
    for (i = 0; i <= n; i++)
    {
        
    }
}
void main()
{
    int arr[max];
    printf("Enter the Lenght of the array : ");
    scanf("%d", n);

    for (int i = 0; i < n; i++)
    {
        printf("Enter the value of the array %d: ", arr[i]);
        scanf("%d", arr[i]);
    }
}